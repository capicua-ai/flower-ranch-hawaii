# SOUPED.md

This file tracks every decision made on this project regarding the Souped suite — any tool, skill, or agent under the `souped` plugin (auth, design, Chalk, Carte, Spark, Shelf, Fond, and anything added later).

## USE IT AS A GUIDE, NOT AS GROUND TRUTH

It speeds up agents by pointing at what was done previously and where to look. But it can drift: someone may have changed signup restrictions, added a Chalk table, or rotated a Vercel domain outside of the orchestrator's flow.

**Rules for ANY agent or skill in the `souped` plugin:**

1. **Read this file at the start of every session** for orientation about prior decisions.
2. **Before acting on a specific value, verify it against the real source** — the appropriate Souped MCP tool, the codebase, or the database. Example: if a recent entry says signup restriction is `open` and you need that value to do something, call the Glaze MCP and confirm. **Do not assume.**
3. **If verification reveals a mismatch:** corroborate the new value with a second source so you're confident it's real, then append a `drift detected` entry to the log (see format below) **in the same change as your action**.
4. **After ANY Souped operation, append one entry to the Decisions log below.** This is the only structured place you record what happened — there are no per-subsystem sections to fill in. The log itself is the project's memory.

---

## Project meta

- **Name:** Flower Ranch Hawaii
- **SOUPED.md created on:** 2026-06-23
- **Repo URL:** https://github.com/capicua-ai/flower-ranch-hawaii
- **Souped project ID:** 5c68ad6f-b655-450e-9f71-cde30db3551c
- **Workspace slug:** ws-blue-moss-N6xAsK (Pablo's Workspace)

Anything else (Vercel project, primary domain, current auth scope, registered Chalk tables, Carte pages, Shelf buckets, design contract location, env-var names, etc.) lives in the Decisions log. Don't carve out subsystem sections here — let the log be the single timeline.

---

## Decisions log

Append-only. Newest entries at the bottom. One entry per Souped operation.

**Format:**

```
YYYY-MM-DD — <tool-or-subsystem> — <short title>
<1–3 sentences explaining what happened and why. Mention values an agent might
want to verify later (route patterns, table names, role names, domain, env
var names — never values).>
```

**Examples** (illustrative — replace with real entries as the project evolves):

```
2026-06-19 — scaffolder — initial scaffold
Cloned from souped-boilerplate. Stack: Next.js 16 + Prisma 7 + Souped auth.

2026-06-19 — souped-mcp — Souped project created
Project ID abc123 in workspace acme. Audience: webapp:abc123.

2026-06-19 — auth-scaffolder — DB-backed users wired
auth_scope=global (matcher unchanged). Added Prisma User model with roles
enum [admin, member]. requireRole helper in src/lib/auth.ts.

2026-06-19 — chalk — registered 'faqs' table
Collection of FAQ items. Consumed by /about page (FaqList component).
Editable from Souped → Chalk.

2026-06-19 — carte — registered '/' SEO entry
Public landing. Robots: index. OG image at /og/landing.png.

2026-06-19 — spark — first deploy
Vercel project: acme-app. URL: https://acme-app.vercel.app. Env vars set:
DATABASE_URL, SOUPED_CLIENT_ID, SOUPED_CLIENT_SECRET, SOUPED_SESSION_SECRET.

2026-06-19 — drift detected — signup restriction
SOUPED.md log implied 'open' (no prior entry stating otherwise). Glaze MCP
returned 'domain-allowlist' (acme.com). Confirmed via dashboard. Logging
correction so future sessions see the current state.
```

### Entries

- 2026-06-23 — meta — SOUPED.md initialized from boilerplate. Will track every Souped-suite decision on this project from now on.
- 2026-06-23 — scaffolder — initial scaffold. Cloned souped-boilerplate (Next.js 16 + Prisma 7 + Souped auth). Repo: capicua-ai/flower-ranch-hawaii (private). Local: /Users/capicua/Desktop/Claude/flower-ranch-hawaii.
- 2026-06-23 — souped-mcp — Souped project created. ID 5c68ad6f-b655-450e-9f71-cde30db3551c (slug pj-kind-lake-zLSLSF) in workspace ws-blue-moss-N6xAsK. client_id/secret issued; not yet placed in env (auth wiring pending).
- 2026-06-23 — spark — database created. Neon Postgres provisioned for the project. DATABASE_URL stored in local .env.local only.
- 2026-06-23 — spark — deploy DEFERRED. create_vercel_project failed: Vercel token on Hobby plan can't deploy a private org-owned repo (capicua-ai). User chose to defer Vercel + deploy until billing is resolved. No Vercel project exists yet.
- 2026-06-23 — design — design contract locked (see DESIGN.md). Palette: lime #8ED85F (action only), deep teal #004655 (dark surfaces/ink), pale teal wash #E8FCFF + neutral teal-gray border #CDE3E6 (structural — vivid never on structural tokens, per v2 retrospective). Fonts: Inter + JetBrains Mono (matching v1). Applied to src/app/globals.css + layout.tsx.
- 2026-06-23 — environment — project lives in DEV Souped, not prod. The connected Souped/Spark MCP (e1f77d53) targets the dev environment, so project 5c68ad6f + its database are dev-only. Confirmed: visible in dev Souped dashboard, not prod. Fine while building locally (deploy deferred). When promoting to prod: connect the prod Souped plugin, recreate project + DB in prod, wire prod SOUPED_* creds. Do not assume the project ID/DB carry over to prod.
- 2026-06-23 — prisma — migration init_storefront_content. Added models: SiteSettings (singleton), Product, BlogPost, Benefit, DeliveryStep (tables site_settings, products, blog_posts, benefits, delivery_steps). Seeded via spark run_sql. Note: prisma CLI loads .env not .env.local — pass DATABASE_URL inline for migrate/generate. After migrate, RESTART the dev server (it caches the generated client).
- 2026-06-23 — auth — wired Souped auth (scope: specific routes). Enabled auth on dev project; redirect URI http://localhost:3000/api/auth/callback. SOUPED_* + SOUPED_SESSION_SECRET in .env.local (NOT yet on Vercel — deploy deferred). Added Prisma User model (table users, migration add_user) synced from JWT sub/email/roles. src/lib/auth.ts: getOrCreateUser (lazy upsert), requireUser, requireRole. Protected routes: /account, /checkout, /orders (NOT in proxy publicRoutes); storefront stays public. Verified: /account & /checkout → 307 /api/auth/login; public routes 200. The scaffolder agent was launched but paused on approval and could not be resumed (no resume tool surfaced); orchestrator completed the wiring directly per the agent's stated plan.
- 2026-06-23 — chalk — registered 5 tables for the storefront. site_settings (singleton: hero copy + contact email), products (collection, full CRUD; specs/gallery kept as code-managed JSON), blog_posts (collection, full CRUD, body=markdown), benefits (collection, create/delete LOCKED — fixed 5, icon=select), delivery_steps (collection, create/delete LOCKED — fixed 5, icon=select). Pages read from DB via src/lib/store-data.ts (now DB-backed); icon strings → lucide via src/lib/icon-map.ts. Editable at Souped → Chalk.
- 2026-06-23 — deploy constraint (IMPORTANT) — this v2 build MUST NOT replace or overwrite the existing v1 reference site. v1 is a SEPARATE project: code at ~/Documents/Antigravity/flower-ranch-hawaii, its own repo, live at https://flower-ranch-hawaii.vercel.app, its own Vercel project. v2 = repo capicua-ai/flower-ranch-hawaii, not yet deployed. When deploying v2: create a NEW Vercel project with a DISTINCT name + NEW URL; never deploy into v1's Vercel project and never reassign v1's `flower-ranch-hawaii.vercel.app` domain (or any v1 domain) to v2 unless the user EXPLICITLY says so. We have only ever read v1 as a design reference — never written to it.
- 2026-06-24 — auth — `/checkout` made PUBLIC (temporary). Added `/checkout` to proxy `publicRoutes` so the new client-side cart review (no payment, no sensitive data — cart lives in localStorage) is reachable without login. `/account` and `/orders` stay protected. Revert (remove `/checkout` from publicRoutes) when a real payment/account flow lands. See note in src/proxy.ts.
- 2026-06-24 — feature — functional cart added (no payments). Client cart context (src/components/store/cart-context.tsx, persisted to localStorage key `fr-cart-v1`) + cart-drawer, card-add-button, product-buy-buttons, checkout-client. Header shows live count; /checkout is a preview review with a placeholder "Place order" (clears cart, no charge). Real payment (Stripe or other) DEFERRED until provider/shipping/tax are decided.
- 2026-06-24 — spark — v2 DEPLOYED to its OWN NEW Vercel project (v1 untouched, per the deploy constraint above). Project name `flower-ranch-hawaii-v2`, Vercel id prj_Oft0iNzPGsq19VhfgSXc2uH01yMg, Souped project 5c68ad6f. Live URL: https://flower-ranch-hawaii-v2.dev.getsouped.app. All 9 env vars (DATABASE_URL [dev Neon], NEXT_PUBLIC_APP_URL, SOUPED_URL/CLIENT_ID/CLIENT_SECRET/APP_ID/AUDIENCE/ISSUER/SESSION_SECRET) set on Vercel. DB is still the DEV Neon (shared preview, not prod). Deploy flow: auto-deploy triggers on pushes to `main` only; we work on `pablo/ecommerce-storefront`, so to deploy we fast-forward main to it (`git push origin pablo/ecommerce-storefront:main`). The Spark manual-deploy tool rejected `ref` (target error), so main-push is the working path. Build fix needed: cast Prisma Json fields via `unknown` in store-data.ts (next build type-checks; Turbopack dev did not). AUTH redirect URIs in Souped NOT yet updated for this URL — /account & /orders login will fail until https://flower-ranch-hawaii-v2.dev.getsouped.app/api/auth/callback is added via glaze_update_redirect_uris; storefront + cart + /checkout are public and work without it.
- 2026-06-25 — glaze — redirect URIs updated (login now works on the live v2). allowedRedirectUris now = ["http://localhost:3000/api/auth/callback", "https://flower-ranch-hawaii-v2.dev.getsouped.app/api/auth/callback"]. authEnabled=true confirmed. NOTE: glaze_update_redirect_uris REPLACES the whole list — include every URI when editing. Login UX = account icon → /account (gated → Souped login); /account shows email + "Sign out" (/api/auth/logout). No separate Sign-in button in the nav by design.
- 2026-06-25 — spark — v2 redeployed (design iteration batch). Pushed commit 51afa6b to `pablo/ecommerce-storefront` then fast-forwarded `main` (the working deploy path). Vercel deployment dpl_7USthjwksawKob8DzBDhDXwRU1Q9 → state READY, build 28s (compiled + TypeScript OK, 15 routes; /products now ƒ dynamic due to `?q=` search). Changes: hero_video2 bg @0.65x + 0.25px blur (HeroVideo client component forces play()), floating closing CTA (banner.png) inside the story section, numbered roadmap + dimensional mint circles, benefits bento icons removed + shorter cards, nutrition tile = 4 equal count-up stats (2×2, vertical divider), navbar in-pill animated search → /products?q= with filtering + empty state, orchard video = placeholder_video. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-25 — drift detected — Spark MCP workspace. SOUPED.md records this project under workspace ws-blue-moss-N6xAsK. One connected Spark MCP now reports different workspaces (ws-eager-ash-kNWYj3, ws-past-isle-7_h1G0) with no Vercel creds; the deployment-logs call only succeeds via the MCP instance still bound to ws-blue-moss-N6xAsK. Future sessions: use the ws-blue-moss-N6xAsK-bound Spark connection for this project's Vercel ops, or reconfigure credentials.
- 2026-06-26 — spark — v2 redeployed (motion + mesh batch). Commit 249b04f → main (FF). Vercel dpl_2Z156KtCQq5DLAvKfJ8di8PapExk → READY, build 20s (compiled + TS OK, 15 routes). Changes: seamless ping-pong hero loop (hero_video2_loop.mp4, generated via ffmpeg fwd+reverse concat) + 0.25px blur; new MeshBackground component (grainy aurora mesh — lime/green/teal blobs blur(100px) + film grain, intensity 0.5, grain 0.8) on Benefits & Our Story, Products/Blog kept white for rhythm; new InView component (subtle scroll-reveal on section headers/grids); RoadmapSteps client component (flowing dashed connector via fr-flow keyframe + staggered 01→05 reveal, flat circles, cream halo removed); pill CTAs dropped the arrow icon (kept on text links). New CSS in globals.css: fr-search-in, fr-flow, fr-step/fr-steps-in, fr-inview (all gated by .js + reduced-motion). Note: Turbopack dev needs a `.next` clear + restart to pick up new globals.css rules. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-26 — spark — v2 redeployed (botanical illustrations). Commit c22e490 → main (FF). Vercel dpl_6hZz4GERpdUnFJS5pH7fLvAPcdoM → READY, build 23s. New CornerBotanical component (corner-anchored edge-bleeding decorative art, aria-hidden, -z-10, hidden on mobile). Added user-supplied painterly PNGs (public/assets/bg-asset-1..4.png, transparent): a new "Rooted in Hawaiʻi / Grown on the Hāmākua Coast" 2-col editorial band (image farm-bg.png + text) framed by branch art on fr-wash bg; subtle corner accents on Blog (leaf + fruit cluster) and on the Benefits bento (low-opacity branches over the mesh). Sizing via inline style — Turbopack dev doesn't reliably generate arbitrary `w-[%]` classes. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-26 — spark — v2 redeployed (interior-header art). Commit 7a8875a → main (FF). Vercel dpl_64FugbSxm5LAzG1YfoepE2gTGKCi → READY, build 22s. Added CornerBotanical (bg-asset-2 branch) bleeding from the right of the teal page headers on /products and /blog (relative isolate overflow-hidden, content set relative). /wholesale skipped (video-bg hero, hidden from nav). v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-26 — spark — v2 redeployed (hover tweak). Commit 19eb91e → main (FF). dpl_9opHtj3SLdJQzNs7Ntiiuu8gxawf READY, build 24s. Step-card hover no longer zooms the photo (kept lift + lime ring + icon scale/tint). v1 untouched.
- 2026-06-26 — spark — v2 redeployed (step-card polish). Commit a02dffa → main (FF). Vercel dpl_CENwPJxGuots8ZpCgDxGqePKP5QP → READY, build 32s. RoadmapSteps cards refined: photo now fills the remaining card space with no white gap (img is `absolute inset-0` inside a `flex-1` wrapper with inline min-height — a plain `h-full` img ballooned the grid row to its intrinsic portrait height, ~634px); equal cards ~426px; more gradual top fade (176px, 3-stop); roomier icon→title→copy spacing (p-6, mt-6, mt-3). Our Story mesh lightened (base #fbfdf8, MeshBackground intensity 0.32). v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-26 — spark — v2 redeployed (photo step cards). Commit db2e892 → main (FF). Vercel dpl_9A6ViKhuYDz2D9WVAGKVkYMxM2sh → READY, build 23s. Step cards now show real photos (public/assets/step1-5.png) at the bottom of each card with a white top fade, 176px frame, and per-image object-position (STEP_FOCUS) so each subject (truck/box/fruit) is framed. Step number moved into the card header (same row as the icon); removed the dashed connector/timeline (both horizontal + mobile vertical). v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-26 — spark — v2 redeployed (roadmap redesign + ping-pong hero_video). Commit 7feab31 → main (FF). Vercel dpl_2xdrgxK4yT82VdpzMPPXhRRmsmS3 → READY, build 21s. RoadmapSteps reworked into horizontal glass cards (icon disc + numbered node + title + body; flowing dashed connector horizontal on desktop / vertical on mobile; equal-height translucent cards w/ backdrop-blur + top sheen + hover icon-scale/lime-tint). Scroll reveal switched from CSS transition to keyframe (fr-rise) because transitions get stuck mid-flight here (constant repaints from playing video + blurred mesh) — same fix pattern as the navbar search (fr-search-in). Hero now hero_video_loop.mp4 (ping-pong of original hero_video via ffmpeg). Benefits feature body forced to one line; trimmed extra botanical accents (Benefits bottom-right, Blog leaf). New globals.css: .fr-step-icon hover tint, fr-step reveal now keyframe. Recurring note: Turbopack dev needs `.next` clear + restart to pick up new globals.css rules AND newly-introduced utility classes (e.g. h-14, bg-white/70, w-[%]) — those were moved to inline styles. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-26 — spark — v2 redeployed (step5 photo swap). Commit ca452d1 → main (FF). dpl_61yqdSZpkFbJBTihDfD1GL6GRDQe READY, build 22s. Replaced public/assets/step5.png (kitchen/bowl scene) and refocused its STEP_FOCUS crop to 50% 72%. v1 untouched.
- 2026-06-29 — spark — v2 redeployed (step1 + step4 photo swap). Commit 50be1f0 → main (FF). dpl_GvhSP7v1CPiEJuB5YT1nrHjKon4E READY, build 24s. Replaced public/assets/step1.png (longan branch over the ocean/coastline) and step4.png (Flower Ranch truck on the road); refocused STEP_FOCUS[0] 82%→58% to center the fruit cluster (step4 kept at 80%). Note: an unused public/assets/step1b.png was left out of the commit. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-29 — spark — v2 redeployed (orchard video v2 + play-on-view). Commit 331fbd6 → main (FF). dpl_4PQURkRyUa6YNJ6eGCAz9PFdPBf4 READY, build 24s. "From our orchard to your table" video swapped to public/assets/placeholdervideo_v2.mp4 (~5.7MB) and now plays at 0.9x, gated on visibility: HeroVideo gained an optional `playOnView` prop (IntersectionObserver threshold 0.3 → play when ≥30% visible, pause when out of view; autoPlay attr off in that mode). Hero (page.tsx) unchanged (still autoplays). step1b.png still left out. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-29 — spark — v2 redeployed (orchard video freeze + delayed auto-replay). Commit 03cd393 → main (FF). dpl_7UojkzQ7vgkVTWqnA7nnekjFMioG READY, build 24s. Reworked the orchard-video replay UX: dropped the interim "Watch again" button/scrim overlay, and gave HeroVideo a `replayDelayMs` prop instead. With it the clip plays once (native loop off), freezes on its last frame, then auto-replays after the delay (set to 15000ms here) — looping with a 15s breath. Replay is visibility-aware: if the clip is off-screen when the timer fires, it defers and replays when it next scrolls back in (visibleRef + pendingReplayRef gate). Hero (page.tsx) unchanged. step1b.png still left out. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-29 — spark — v2 redeployed (video v3 + corner blur, nav cleanup, ecommerce login). Commit b729337 → main (FF). dpl_6VHKK9WLAP9wzNiwkC2vFMMdBXT4 READY, build 23s (16 routes incl. new /account/login). Changes: (1) orchard video → placeholdervideo_v3.mp4 (~3.9MB); (2) Gemini watermark hidden via 4 feathered corner blur patches (backdrop-blur + radial mask) over the video — 3 subtle (blur 7px, solid core 12%), bottom-right stronger (blur 13px, solid 26%) to dissolve the burned-in mark; chose this over a botanical overlay (read as a sticker over video) and over cropping (user found zoom too tight); (3) navbar: removed the search icon + its in-pill takeover entirely, dropped "Shop" link, renamed "Our Story"→"Our Process" after Benefits (nav now Benefits · Our Process · Blog); (4) new ecommerce-style sign-in page at /account/login (Email + Password fields + Forgot password + SIGN IN/CREATE ACCOUNT, restyled to site button language — lime pill + bordered pill, rounded-xl inputs). IMPORTANT: the email/password fields are PRESENTATIONAL only — auth is Souped OAuth, so the buttons route to /api/auth/login (the real hosted credential page); we do NOT collect passwords in-app. A native password form would require switching off Souped OAuth (escalate first). Account icon now → /account/login; /account/login added to proxy publicRoutes; /account stays gated. Login page passed the design-signature bouncer (atomic-inspiration: mobbin + minimal.gallery; .gate-manifest.json committed). step1b.png still left out. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-29 — spark — v2 redeployed (soften corner blurs). Commit 9fad524 → main (FF). dpl_4FxouPjMocWMAn7PzDtztZjQ7Vu9 READY, build 28s. Lowered the three subtle video-corner blurs (top-left/top-right/bottom-left) from blur 7px/solid 12% to blur 4px/solid 10%; bottom-right kept strong (blur 13px, solid 26%) so it still hides the Gemini watermark. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-29 — spark — v2 redeployed (orchard video loop + subtle zoom). Commit 4826f14 → main (FF). dpl_62LSM16t8qaz28GHfa3d1PtaPFPR READY, build 23s. Orchard video reverted to continuous native loop (removed the freeze-on-last-frame + 15s replayDelayMs; HeroVideo `replayDelayMs` prop now unused) and given a subtle `transform: scale(1.07)` zoom via HeroVideo's `style` prop. Still 0.9x + playOnView. Corner-blur watermark cover unaffected (watermark stays near the corner under the reinforced bottom-right patch). v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-06-29 — spark — v2 redeployed (corner-blur seam fix on wide screens). Commit 385df05 → main (FF). dpl_6zQQQjmAmwMbgthSQaQnc2eYTimB READY, build 24s. Fixed a visible rectangular seam from the video corner-blur patches on wide monitors: the radial mask faded to transparent at 100% (= farthest corner only), leaving the box's near edges partially opaque (~39% at the ~71% near-edge radius) → a faint straight line. Changed the transparent stop to 68% so the mask hits zero before any box edge, killing the seam. Watermark sits at ~31% radius in the bottom-right patch, still fully covered. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-07-01 — spark — v2 redeployed (client-feedback batch). Commit 466f07b → main (FF). dpl_BrHT1kz4txVcFG89ym6QjzB8ubNN READY, build 23s (16 routes). Client email items: (1) NEW "How to eat longan" section on the homepage (src/components/store/how-to-eat.tsx) — numbers-only step cards (crack → peel → enjoy → discard seed) + a "Watch how" button linking the client's reference YouTube video; placed between Products and Our Story. (2) B2B → footer link only (per client "just a link at the bottom"): added "Wholesale (B2B)" to the footer Company column (also fixed the footer's stale "Our Story" label → "Our Process"); decided NOT to add wholesale to the top nav (kept the consumer/B2C nav clean). Plus design polish: rounded-bottom "stacked panel" section transitions on the HOMEPAGE only (Benefits→Products→How-to-eat→Our Story→Blog→Footer, hero stays flat; overlap via -2.5rem margins + z-index all kept < the sticky nav's z-50; interior pages left flat for now — open question whether to extend). Wholesale page alignment pass: hero bg swapped video → hero-bg.png (dropped Hero video-2.mp4 ref); product image frame fixed (removed p-8 + bg-white/5 letterbox → object-cover 4/5 rounded card); stats now use CountUp (300K/50+/+$20); Quality/Values/contact cards upgraded from flat border to the site's shadow+ring elevated style; Quality icon discs rounded-2xl → rounded-full + step number → #33971f; content fix "Kona" → "Hilo, Hawaiʻi" to match the rest of the site. Print/brand collateral (logos PNG, business cards, brand book, clamshell sticker, Longan box) still PARKED pending assets. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-07-01 — spark — v2 redeployed (how-to-eat video → modal). Commit 326b019 → main (FF). dpl_2xGWE8bCPSLBu78XXL8xFD8z5LAd READY, build 21s. The "Watch how to open & eat" button now opens the reference YouTube video in an accessible lightbox modal (new client component src/components/store/video-modal-button.tsx: embedded iframe autoplay, closes on X/backdrop/Esc, body-scroll lock, iframe unmounts on close so playback stops) instead of navigating to YouTube. how-to-eat.tsx now passes videoId="Ts7RPpYbi8c" to it. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-07-01 — spark — v2 redeployed (how-to-eat photos + polish batch). Commit a120332 → main (FF). dpl_BvW4LiGaTYiDKKDHKFGhZWLobXk2 READY, build 23s. Changes: (1) how-to-eat step cards — video "Watch" button REMOVED; each card got a 4:3 image (public/assets/use1–4.png, object-cover, rounded) and the step number + title now share one row (was stacked, wasted vertical space); the VideoModalButton component is now unused/parked. (2) Smooth scrolling scoped to in-page anchors ONLY — removed the global `html { scroll-behavior: smooth }`; new client component src/components/store/smooth-anchors.tsx intercepts same-page hash links (#id or /#id on home) and smooth-scrolls (reduced-motion aware); page navigation / programmatic scrolls are now instant. (3) Wholesale hero image → public/assets/wholesalehero.png; "The Values" section now uses the home's grainy MeshBackground (over #f7faf0) instead of flat bg-fr-wash. (4) Footer logo now matches the navbar lockup: generated public/assets/fr_logo_footer.svg from fr_logo_navbar.svg (wordmark ink #1A1A1A → #FFFFFF, green mark kept) for the dark teal footer, replacing the old logo.png tagline lockup. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-07-01 — spark — v2 redeployed (how-to-eat card motion). Commit 5e51b53 → main (FF). dpl_HE865RydeB1aYDRrTSEBeUFxS3Tx READY, build 23s. Added motion to the how-to-eat step cards: each card is wrapped in InView with a staggered delay (i*90ms) for a sequential fade+rise scroll-in reveal, and gained a hover state (card lift -translate-y-1.5 + lime ring + deeper shadow, gentle photo scale-105 zoom). Reuses existing InView + Tailwind hover utilities (reduced-motion aware). v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-07-02 — spark — v2 redeployed (new brand logos + favicon). Commit 5b4cee6 → main (FF). dpl_8gNnD27vkmUaTTWu4uqHK3McocHy READY, build 21s. Client-supplied logos swapped in: navbar + login card → public/assets/logo_web_blacktext.svg (dark, light surfaces); footer → public/assets/logo_web_white.svg (teal surface). Retired the interim fr_logo_navbar.svg/fr_logo_footer.svg references (files still in assets, unused). Favicon updated: copied the new public/assets/favicon.svg over public/favicon.svg (the referenced icon), so no metadata change needed. Note: browsers cache favicons hard (hard-refresh to see). apple-touch icon still /assets/logo.png (left as-is). v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-07-03 — spark — v2 redeployed (wholesale = standalone B2B mini-site). Commit d11fadd → main (FF). dpl_92sbi4itiAT4g91GjNAckZS4kdBP READY, build 29s. Reworked /wholesale into a self-contained B2B "site" mirroring v1 (flower-ranch-hawaii.vercel.app, read-only reference) but in our current style: (1) NEW dedicated navbar src/components/store/wholesale-header.tsx — same floating-pill style as SiteHeader but B2B in-page anchor links (Opportunity · Story · Product · Quality · Values · Contact) + a lime "See our products" CTA → /products; replaces SiteHeader on /wholesale only. (2) Added the missing "Story" section (5 decades family farming, Hāmākua ridge; bg-fr-cream 2-col with hero-bg.png) and gave every section an anchor id (top/opportunity/story/product/quality/values/contact) so the nav scroll-spies via the existing SmoothAnchors handler; order now matches v1. (3) Symmetric footer: SiteFooter gained a `variant` prop — variant="wholesale" swaps the self-referential "Wholesale (B2B)" Company link for "Retail store" → / (opens new tab). DTC footer keeps "Wholesale (B2B)" → /wholesale (also new tab, added earlier this session). (4) Upgraded the wholesale hero: min-h-[112vh] (matches home), left-weighted teal scrim + lime radial glow, bigger serif headline (text-7xl), staggered Reveal entrance motion. (5) New imagery: wholesalehero.png (hero) + productsectionimage.png (Product section, replacing "Image product stack.png"). v1 untouched (only read as reference). Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-07-03 — spark — v2 redeployed (asset refresh). Commit 85b9075 → main (FF). dpl_9Emur7frzkbqCTvL6Scdybz9uz9G READY, build 21s. Swapped two images (no code change — same filenames): public/assets/Opengraph.jpg (new social share image; metadata in layout.tsx already points to it — note social platforms cache OG hard, re-scrape via FB/LinkedIn debuggers after deploy) and public/assets/step4.png (updated roadmap truck photo; existing STEP_FOCUS[3] "50% 80%" still frames it). v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-07-03 — spark — v2 redeployed (wholesale Story image). Commit 08fd817 → main (FF). dpl_2iggrbHCKfHNa4eJzFtvmCJvh8Ap READY, build 24s. Wholesale "The Story" section image swapped from the placeholder hero-bg.png to public/assets/fivedecades.png (dedicated Hāmākua orchard/pastoral shot). Same rounded 4:3 frame. v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
- 2026-07-03 — spark — v2 redeployed (wholesale polish). Commit ded8459 → main (FF). dpl_3JM44SJ5Pzs4ZJAM1JwuqMCjJ1KC READY, build 29s. (1) New animated Marquee component (src/components/store/marquee.tsx) replacing the static lime brand strip — seamless infinite scroll (items duplicated, translateX -50% via embedded keyframe), dot (•) separators, bigger text (text-base), py-5, pause-on-hover, reduced-motion safe, no edge gradient. (2) Wholesale navbar "See our products" CTA now opens /products in a new tab (desktop + mobile). (3) Values heading "?" moved inside the green accent. (4) Contact-form dropdowns: bumped px-4→px-5 and replaced the native browser chevron with a custom lucide ChevronDown (appearance-none + pr-11). v1 untouched. Live: https://flower-ranch-hawaii-v2.dev.getsouped.app
